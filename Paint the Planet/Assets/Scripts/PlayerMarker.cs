using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMarker : MonoBehaviour
{
    Transform planet;

    // Update is called once per frame
    void Update()
    {
        //TODO: 
        //planet = GameObject.FindGameObjectWithTag("Player").GetComponent<Gravity>()._PlanetTransform;
        //transform.position = new Vector3(planet.transform.position.x, 200, planet.transform.position.z);
    }
}
